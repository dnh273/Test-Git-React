import React, { Component } from 'react'

export default class Header extends Component { // React class components

    render() { // render là phương thức hiện thị nội dung của component
        return (
            <div>

                <h1>
                    Hello header components
                </h1>

            </div>
        )
    }
}
